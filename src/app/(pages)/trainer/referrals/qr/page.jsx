import ReferralQrCard from "@/components/earnings/ReferralQrCard";
export default function TrainerReferralQrPage() {
  return <ReferralQrCard codeQr subtitle="Print it, download it, or share the link — every client who signs up through it is credited to you." />;
}
